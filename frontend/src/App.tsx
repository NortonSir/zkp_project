import React from 'react';
import LoginForm from './components/LoginForm';
import './index.css'; // Tailwind CSS를 위한 전역 CSS 파일 (보통 index.css에 포함)

function App() {
  return (
    <LoginForm />
  );
}

export default App;
